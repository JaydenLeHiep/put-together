name: seashell-app

services:
  - name: put-together-backend
    github:
      repo: JaydenLeHiep/put-together
      branch: main
      deploy_on_push: false
    dockerfile_path: backend-put-together/Dockerfile
    http_port: 8080
    routes:
      - path: /api
